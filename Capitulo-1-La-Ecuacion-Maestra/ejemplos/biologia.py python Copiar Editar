# Pulso nodal ADN — frecuencia viva
import numpy as np
import matplotlib.pyplot as plt

t = np.linspace(0, 5, 1000)
f = np.sin(2*np.pi*1.8*t) * np.exp(-0.3*t)
plt.plot(t, f)
plt.title("Pulso bio-nodal (ADN)")
plt.xlabel("Tiempo (s)")
plt.ylabel("Intensidad")
plt.grid()
plt.show()
